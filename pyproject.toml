[project]
name = "bias-ext-mentions"
version = "0.1.0"
dependencies = ["bias-core>=0.1,<0.2"]
[project.entry-points."bias.extensions"]
mentions = "bias_ext_mentions.backend.ext:extend"
[tool.setuptools.packages.find]
where = ["."]
include = ["bias_ext_mentions*"]
